services:
  app-dev:
    build:
      context: .
      target: dev
    container_name: animedle-app
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "5173:5173"
    depends_on:
      - mongo
    environment:
      - name=value

include:
  - docker-compose.yml
